<?xml version="1.0" encoding="UTF-8" ?>
    <!DOCTYPE mapper
            PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
            "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- <mapper namespace="com.wj.entity.StudentMapper">
    　　id ="接口中的方法名"
　　parameterType="传入的参数类型"
　　resultType = "返回实体类对象，使用包.类名"-->

<mapper namespace="com.wj.mapper.StudentMapper">

    <select id="quarry" resultType="com.wj.entity.Student">
          select * from student
        </select>

    <select id="quarrybyid" resultType="com.wj.entity.Student" parameterType="int">
          select * from student where stuno=#{stuno}
        </select>

    <insert id="add" parameterType="com.wj.entity.Student">
         insert into student(stuno,stuname,stuage,gradename) values (#{stuno},#{stuname},#{stuage},#{gradename})
       </insert>

    <update id="update" parameterType="com.wj.entity.Student">
        update student set stuname=#{stuname} where stuno=#{stuno}
    </update>

    <delete id="delete" parameterType="int">
        delete from student where stuno=#{stuno}
    </delete>

</mapper>
